# Phase 1 — Symptom Collector Agent
langgraph>=0.2.0
langchain>=0.3.0
langchain-groq>=0.2.0
langchain-core>=0.3.0
python-dotenv>=1.0.0

# Phase 1 — API Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic[email]>=2.0.0
python-multipart>=0.0.9

# Phase 2 — PDF Report Generator
reportlab>=4.0.0

# Phase 4 — Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Phase 4 — Supabase Auth (replaces passlib + python-jose)
supabase>=2.0.0